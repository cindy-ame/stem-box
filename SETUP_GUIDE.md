# Learning App 設定指南

## 步驟總覽

```
步驟 1: 安裝套件 ───────────── 5 分鐘
步驟 2: 建立 Supabase 專案 ── 10 分鐘
步驟 3: 設定環境變數 ───────── 2 分鐘
步驟 4: 啟動 App ───────────── 1 分鐘
```

---

## 步驟 1: 安裝套件

打開終端機，執行：

```bash
cd ~/projects/learning_app
npm install
```

等待安裝完成（可能需要 2-3 分鐘）。

---

## 步驟 2: 建立 Supabase 專案

### 2.1 登入 Supabase

1. 打開瀏覽器，前往 https://supabase.com
2. 點擊右上角 **Sign In**
3. 用你的 GitHub 帳號登入（或其他方式）

### 2.2 建立新專案

1. 點擊 **New Project**
2. 填寫資料：
   - **Name**: `learning-app`
   - **Database Password**: 自己設一個密碼（記下來！）
   - **Region**: 選 `Northeast Asia (Tokyo)` 最近
3. 點擊 **Create new project**
4. 等待 2-3 分鐘讓專案建立完成

### 2.3 取得 API 金鑰

1. 在左側選單點擊 **Project Settings**（齒輪圖示）
2. 點擊 **API**
3. 你會看到兩個重要資訊：
   - **Project URL**: `https://xxxxxx.supabase.co`
   - **anon public key**: 一串很長的字

**把這兩個複製下來！**

---

## 步驟 3: 設定環境變數

1. 在 `~/projects/learning_app/` 資料夾建立檔案 `.env.local`
2. 貼上以下內容（替換成你的值）：

```
NEXT_PUBLIC_SUPABASE_URL=https://你的專案ID.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的anon-key
```

### 用終端機的方式：

```bash
cd ~/projects/learning_app

# 建立 .env.local 檔案
cat > .env.local << 'EOF'
NEXT_PUBLIC_SUPABASE_URL=https://你的專案ID.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的anon-key
EOF
```

記得把 `你的專案ID` 和 `你的anon-key` 換成真實的值！

---

## 步驟 4: 啟動 App

```bash
cd ~/projects/learning_app
npm run dev
```

看到這個訊息就成功了：
```
▲ Next.js 14.0.3
- Local: http://localhost:3000
```

打開瀏覽器，輸入 `http://localhost:3000`

---

## 🎉 完成！

你應該會看到 Learning App 的首頁。

---

## 常見問題

### Q: npm install 失敗？
```bash
# 清除快取後重試
rm -rf node_modules package-lock.json
npm install
```

### Q: 找不到 .env.local？
這是隱藏檔案，用終端機可以看到：
```bash
ls -la ~/projects/learning_app/
```

### Q: localhost:3000 打不開？
可能 3000 被佔用，試試看 3001：
```bash
npm run dev -- -p 3001
```

---

## 下一步（之後再做）

- [ ] 執行 SQL 建立資料表
- [ ] 設定使用者登入
- [ ] 匯入教材資料

這些我會一步步帶你完成！
